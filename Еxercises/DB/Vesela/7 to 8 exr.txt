
		07
        SELECT person.status, AVG(car.horse_power) as max_horse_power
			FROM person
			JOIN car ON person.car_id = car.car_id
			GROUP BY person.status
 
 
		08
       CREATE TRIGGER person_height_working_decrease BEFORE INSERT ON person
		FOR EACH ROW SET NEW.height = NEW.height - 5;
 